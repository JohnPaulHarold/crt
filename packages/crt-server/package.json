{
	"name": "crt-server",
	"version": "1.0.0",
	"private": true,
	"description": "Server-side rendering utilities for the CRT project.",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/types/index.d.ts",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/types/index.d.ts"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "rm -rf dist && tsc -p tsconfig.json && babel src --out-dir dist --extensions \".ts,.js\" --config-file ../../babel.config.json"
	},
	"license": "Apache-2.0",
	"dependencies": {
		"crt": "file:../crt"
	},
	"devDependencies": {
		"@types/qs": "^6.9.15"
	},
	"peerDependencies": {
		"express": "^5.1.0"
	}
}
