{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"emitDeclarationOnly": true,
		"composite": true,
		"declarationDir": "dist/types",
		"rootDir": "src"
	},
	"include": ["src/**/*"],
	"exclude": [
		"node_modules",
		"dist",
		"../crt/dist" // This is now redundant due to project references but is harmless.
	],
	"references": [{ "path": "../crt" }]
}
