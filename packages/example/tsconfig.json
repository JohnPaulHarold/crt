{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"emitDeclarationOnly": true,
		"composite": true,
		"declarationDir": "dist/types",
		"rootDir": "."
	},
	"include": ["src/**/*", "loader.js", "server.js"],
	"exclude": [
		"node_modules",
		"dist",
		"../crt/dist" // This is now redundant due to project references but is harmless.
	],
	"references": [{ "path": "../crt" }]
}
